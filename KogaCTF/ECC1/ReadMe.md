# 楕円曲線暗号1

## Description
楕円曲線について学びましょう。

体K上で、方程式 y^2 = x^3 + a*x + b を満たす点(x,y)（x,y∈K）全体と、無限遠点Oを含む集合を、K上の楕円曲線といい E(K) と表します。
ただし判別式 Δ ≠ 0 とします。

本問題では SageMath（Sage）を使います。
簡単なコードならWeb上で実行できるのでおすすめです。
https://sagecell.sagemath.org/

さて、
E: y^2 = x^3 + 5*x + 10
P = E(1, 4)
Q = E(3/2, sqrt(334)/4)
のとき、P - Q はいくつですか？

その答えがフラグとなります。

フラグ形式:
CSL24{x:y}

例:
P - Q = (2.5432214? , 5.424125?)
のとき、フラグは
CSL24{2.5432214:5.424125}
となる(最後の?を取ることを忘れないこと)。

```python
E=EllipticCurve(QQbar, [a, b])
#楕円曲線E:y^2=x^3+a*x+b を作るときはEllipticCurve(QQbar, [a, b]) とする.
#問題文から，a,bを適宜置き換えること
#printすると，(x:y:z)のフォーマットに従って表示され，これは射影座標系を採用しているからです．
P=E(1,4)
print("P=",P)
#問題文に記されているQの値を代入
Q=
print("Q=",Q)

print("P-Q=",P-Q)
```
## Solution
